[project]
name = "python"
version = "0.1.0"
description = "Add your description here"
readme = "README.md"
requires-python = ">=3.12"
dependencies = [
    "python-jsonpath>=1.3.1",
]

[dependency-groups]
dev = [
    "mypy>=1.16.1",
]
